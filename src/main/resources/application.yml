server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/kubinex
    username: kubinex
    password: kubinex123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  mail:
    host: ${MAIL_HOST:smtp-relay.brevo.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:b09b99001@smtp-brevo.com}
    password: ${MAIL_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.connectiontimeout: 5000
      mail.smtp.timeout: 3000
      mail.smtp.writetimeout: 5000
      mail.smtp.ssl.trust: smtp-relay.brevo.com

app:
  jwt:
    secret: kubinex-super-secret-key-change-in-production-32chars
    expiration: 86400000

  admin:
    username: admin
    password: ${ADMIN_PASSWORD:change-me}

  mail:
    from: ${MAIL_FROM:lemarchebdi@gmail.com}
    to: ${MAIL_TO:lemarchebdi@gmail.com}

  upload:
    dir: ${user.dir}/uploads

spring.servlet.multipart:
  max-file-size: 10MB
  max-request-size: 10MB
